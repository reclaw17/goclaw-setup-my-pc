# docs/custom

Drop your own Markdown notes here (VPN configs *without secrets*, ISP notes, lab IPs, etc.).

The agent skill `local-docs` reads this folder.  
**Do not** put API keys or private keys in these files.
